<%= form_for student, url: new_student_path, :remote => true do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
        <% student.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div id ="create_form" remote="<%= true %>">
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>

    <div class="field">
      <%= form.label :birthday %>
      <%= form.datetime_select :birthday %>
    </div>

    <div class="field">
      <%= form.label :class_name %>
      <%= form.text_field :class_name %>
    </div>

    <div class="field">
      <%= form.file_field :image %>
    </div>

    <div class="actions" id="create-student">
      <%= form.submit %>
    </div>
  </div>
<% end %>
